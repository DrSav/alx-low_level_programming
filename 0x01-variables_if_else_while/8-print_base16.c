#include <stdio.h>
/**
 * main
 * Return: 0 (successful)
 */

int main(void)
{
	char x = 'a';
	int y = 0;

	while (y<10)
	{
		putchar(y+'0');
		y +=1;
	}
	while (x <= 'f')
	}
		putchar(y+'0');
		x +=1;
	}
	putchar (10);
	return (0);
}

